# hybrid-rag

Retrieval-augmented question answering over technical documents, built incrementally:
dense retrieval, then hybrid retrieval (BM25 + dense), then reranking, then evaluation.

Status: setting up Version 1.